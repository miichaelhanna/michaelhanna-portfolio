# gstack working files — contains local tokens and browse logs, never deploy
.gstack/
# local env / Vercel OIDC token
.env*.local
.env
# local dev-server config for the preview tooling — nothing the site serves
.claude/
